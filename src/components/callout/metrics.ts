/** Card metrics in CSS px, the glass callout's. */
export const callout = {
  /** Maximum width; the card fills its container up to this. */
  width: 560,
  radius: 8,
  /** Icon (lens ring) diameter. */
  icon: 96,
  /** Icon centre from the card's top-left corner. */
  iconX: 64,
  iconY: 64,
  padding: 40,
} as const;
