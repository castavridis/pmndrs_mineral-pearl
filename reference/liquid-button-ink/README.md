# Liquid Button Ink (component study)

The pond that `src/components/ink-sink/liquid-pond.ts` is ported from, kept for diffing: `ink-liquid-button.js` is the `<ink-liquid-button>` custom element (shaders + sim + APCA), `Liquid Button Ink.dc.html` its study page. From the "Lily pad button component study" export.
